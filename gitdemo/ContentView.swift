//
//  gitdemo
//
//  Created by Asset on 14.02.2025.
// created by andamassov

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("my name is asset")
            
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
